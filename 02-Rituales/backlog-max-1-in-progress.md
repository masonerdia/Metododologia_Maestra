# Ritual · Backlog con máximo 1 in_progress
Un backlog priorizado (tipo `feature_list`) con **una sola** tarea en progreso a la vez. Toda instrucción sin entrada → se crea un HOTFIX numerado y entra al [[Pipeline]]. Evita trabajo a medias y multitarea que no cierra.
