# Adaptador — <STACK> [TRANSPORTABLE]

Rellena cada gate con el comando/herramienta de tu stack:
- **Build de producción (gate CI):** <comando que produce el artefacto desplegable>
- **Smoke test post-deploy:** <herramienta> → flujos críticos (login + N pantallas) + assert hidratación/200 + consola limpia
- **Migraciones:** <runner con ledger; aplicar solo pendientes>
- **Staging idéntico:** <cómo levantar la misma imagen/artefacto de prod con datos sanitizados>
- **Gotchas conocidos del stack:** <lista que crece con las lecciones>
